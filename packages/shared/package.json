{
  "name": "@huy-ai/shared",
  "version": "1.0.0",
  "description": "Shared logger, retry backoff utilities and helpers",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
