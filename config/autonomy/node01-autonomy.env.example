# Non-secret runtime settings for Node01 persistent autonomy.
HUY_AI_REPO=/mnt/data1/Projects/HUY-AI-Center
HUY_AI_SUPERVISOR_STATE_DIR=/mnt/data1/HUY-AI/state/supervisors

# Optional ChatGPT autonomous provider.
# Store OPENAI_API_KEY outside Git and expose it to the service environment.
# OPENAI_API_KEY=
# OPENAI_AUTONOMY_MODEL=
# Alternative: an executable that reads the prompt from stdin and writes its answer to stdout.
# CHATGPT_AUTONOMY_COMMAND=
